import java.util.*;
import java.io.*;
  
public static void main(String args[]){
public class Strings {
Scanner sc=new Scanner(System.in);

/*
const  express=require('express');
const app=express();
const socket=require('socket.io');

const server=app.listen(3001,()=>{
    console.log("listening")

})

const io=socket(server,{
    cors:{
        origin:'*'
    }
})
io.on("connection",(socketClient)=>{
console.log(socketClient.id);

socketClient.on("MESSAGE", (clientData)=>{
    console.log(clientData);
    socketClient.emit("MESSAGE","Message received by the server")
})


socketClient.on("BROADCAST", (clientData)=>{
    console.log(clientData);
    io.emit("BROADCAST","Broadcast message for all")

})
socketClient.on("EXCLUSIVEBROADCAST", (clientData)=>{
    console.log(clientData);
    socketClient.broadcast.emit("EXCLUSIVEBROADCAST","EXCLUSIVEBROADCAST message for all")

})
socketClient.on("JOINROOM", (clientData)=>{
    console.log(clientData);
    socketClient.join("CHATTING_ROOM");
    socketClient.emit("JOINROOMSUCCESS","client has joined successfully")
})
socketClient.on("SENDMESSAGEROOM", (clientData)=>{
    console.log(clientData);
    io.to("CHATTING-ROOM").emit("SENDMESSAGEROOM", clientData)
})
})
app.get("/", (req, res) => {
    res.send("Hello");
})
*/
		 
	  
		
			 
			 
		 }
		 
			  
		 
		  
	
		  
		  
		  
	  }
  }